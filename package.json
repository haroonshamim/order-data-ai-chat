{
  "name": "task-0",
  "version": "1.0.0",
  "description": "Client-Server project",
  "scripts": {
    "dev": "cd server && npm run dev",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "dependencies": {
    "axios": "^1.13.6"
  }
}
